# dev

```bash

# start briefing-src-cust, consume port 8080
cd briefing-src-cust
  ./dev.sh
cd ..
# then browse http://localhost:8080/

# start express , consume port 3000
cd docker
  ./build.sh
cd ..

```
