# Please read https://github.com/holtwick/briefing/blob/master/docs/docker.md
# for proper use of Docker

services:
  briefing:
    build: .
    restart: always
    volumes:
      - ./dist:/app/dist
      - ./www:/app/www
      - ./data:/app/data
    env_file:
      - docker-zerva.env
    ports:
      - 8080:8080
# networks:
#   default:
#     external:
#       name: proxy
